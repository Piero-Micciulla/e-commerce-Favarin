<!-- app/views/shops/show.html.erb -->
<h2><%= @shop.name %></h2>
<p><%= @shop.address %></p>
<p><%#= @shop.rating %></p>

<%= link_to "Delete #{@shop.name}",
            shop_path(@shop),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
